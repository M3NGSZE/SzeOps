// if you are using the shared library, you must specified it  here
@library([""])

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {

            }
        }
    }
}